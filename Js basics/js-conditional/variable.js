/**
 * VARIABLE ==> CONTAINER
 */

// number
var sunglassPrice = 80;

// string 
var subject = "EEE";

//boolean
var passed = true;


/**
 * NAMING CONVENTION 
 * 1. single word
 * 2. no quote
 * 3. no gap or dash
 * 4. can not start with number
 * 5. can not be a keyword 
 * 6. prefer camelCase
 */

/**
 * OPERATION
 * 1. +, -, *, /, %
 * 2. +=, -=, *=, /=
 * 3. isNaN ?
 * 4. null
 * 5. Number
 * 6. prefer camelCase
 */

/**
 * isNaN means it is not a number.
 * If the argument passed in the function is not
 * a number then it will show true, or if 
 * the argument passed in the function is a number
 * then it will show false
 */
console.log(isNaN(NaN)) // True, because it is not a number
console.log(isNaN(4)) // False, because it is a number